import { AppModalDirective } from './app-modal.directive';

describe('AppModalDirective', () => {
  it('should create an instance', () => {
    const directive = new AppModalDirective();
    expect(directive).toBeTruthy();
  });
});
